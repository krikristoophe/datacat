# Rust
/target
**/target
*.pdb

# Env / secrets
.env
.env.*
!.env.example
*.pem

# Config réelle (peut contenir des chemins/valeurs locales ; secrets via ${ENV}). Les modèles
# `*.example.toml` et les projets d'exemple sont versionnés.
/datacat.toml
/projects/*.toml
!/projects/example.toml
# Companion agent real config (secrets via ${ENV}); the *.example.toml template is versioned.
/companion/companion.toml

# Site de doc (Astro Starlight)
website/node_modules/
website/dist/
website/.astro/
# Clés de TEST jetables (paires générées, pas des secrets) — versionnées pour les tests.
!backend/tests/fixtures/*.pem
keys/

# Node / TS SDK
node_modules/
sdks/typescript/dist/
examples/web-app/dist/
mcp/dist/
*.tsbuildinfo
.npm/

# Dart / Flutter SDK
sdks/flutter/.dart_tool/
sdks/flutter/.packages
sdks/flutter/build/
sdks/flutter/pubspec.lock

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
